import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.personalfitness.app',
  appName: 'personal-fitness-app',
  webDir: 'www'
};

export default config;
