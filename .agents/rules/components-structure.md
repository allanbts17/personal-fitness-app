---
trigger: always_on
---

Los components creados tienen que tener separación de lógica, template y style. No metas el template dentro del ts.
Si lo que harás se parece a un componente de ionic, intenta usar el componente de ionic.
Todo lo que creas reutilizable guardalo en componente.